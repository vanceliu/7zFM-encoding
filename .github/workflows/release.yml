name: Release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-2022

    steps:
      - uses: actions/checkout@v4

      - name: Extract 7-Zip version
        id: version
        shell: bash
        run: |
          VER=$(grep 'MY_VERSION_NUMBERS' C/7zVersion.h | grep -oP '"\K[^"]+')
          echo "version=$VER" >> $GITHUB_OUTPUT
          echo "7-Zip version: $VER"

      - name: Build 7zFM.exe
        shell: cmd
        run: |
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          cd CPP\7zip\Bundles\Fm
          nmake NEW_COMPILER=1 MY_STATIC_LINK=1

      - name: Prepare release files
        shell: cmd
        run: |
          mkdir release
          copy CPP\7zip\Bundles\Fm\x64\7zFM.exe release\
          copy README.md release\
          copy PATCH_GUIDE.md release\

      - name: Create release archive
        shell: pwsh
        run: |
          Compress-Archive -Path release\* -DestinationPath "7zFM-encoding-v${{ steps.version.outputs.version }}-x64.zip"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: 7zFM-encoding v${{ steps.version.outputs.version }} (based on 7-Zip ${{ steps.version.outputs.version }})
          body: |
            ## 7zFM-encoding v${{ steps.version.outputs.version }}

            Based on 7-Zip ${{ steps.version.outputs.version }}

            ### Features
            - Encoding switch toolbar button for real-time filename codepage switching
            - Supports UTF-8, Shift-JIS, Big5, GBK, EUC-KR, and more

            ### Usage
            1. Download `7zFM-encoding-v${{ steps.version.outputs.version }}-x64.zip`
            2. Extract and run `7zFM.exe`
            3. Open an archive with garbled filenames
            4. Click the Encoding button in toolbar and select the correct codepage

            ### Supported Encodings
            UTF-8, Shift-JIS, Big5, GBK, EUC-KR, ISO-8859-1, Windows-1252, EUC-JP, GB2312, KOI8-R, Windows-1251
          files: |
            7zFM-encoding-v${{ steps.version.outputs.version }}-x64.zip
          draft: false
          prerelease: false
